# List of operating system package dependencies: put one package per line.
# Valid for Debian/Ubuntu only.

# Deps of install.packages()

# Deps of BiocManager::install(c()

# Deps of BiocManager::install()
